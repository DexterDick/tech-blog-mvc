{{#each blogs as |blog|}}
    <article>
        <div>
            <h3><a href="blog/{{blog.id}}">{{blog.title}}</a></h3><p>Posted by
                {{blog.user.username}}
                on
                {{format_date blog.date_created}}</p>

        </div>
    </article>

{{/each}}